# Selector

```ts
const 셀렉터 = createSelector(
	state=> state.events.live // 상태 1 리턴 함수.
  ... // 상태 n 리턴 함수,
  
  // 가장 마지막에는 
  (파라미터1, 파라미터2, ....)=>{ // 각 파라미터에는 위에서 선언한 리턴 함수가 차례로 들어간다.
   // ... 리턴할 값을 로직을 통해 계산한뒤 리턴
	}
)
```